services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: semora
      POSTGRES_USER: semora
      POSTGRES_PASSWORD: semora
    ports:
      - '5433:5432'
    volumes:
      - semora_postgres_data:/var/lib/postgresql/data

volumes:
  semora_postgres_data:
